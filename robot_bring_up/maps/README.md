# Maps Directory

Runtime maps and generated map artifacts for `robot_bring_up` live here.
The directory is installed by the package CMake file, so it must exist even
when no deployment map has been committed yet.
